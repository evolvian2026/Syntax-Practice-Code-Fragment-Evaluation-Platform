import type { SeedQuestion } from './types.js';

/** Python — Conditions (10). */
export const PYTHON_CONDITIONS: SeedQuestion[] = [
  {
    qid: 'PY-CONDIT-0001',
    language: 'python', topic: 'conditions', subtopic: 'if', difficulty: 'Easy',
    type: 'COMPLETE_CONDITION', evaluation: 'OUTPUT',
    title: 'Write the condition: is it even?',
    statement: 'Write the condition that checks whether `num` is an even number.',
    instructions: 'The `if` and the colon are already provided — write only the condition between them.',
    objective: 'Express divisibility as a boolean condition.',
    starter: '{{SETUP_CODE}}\n\nif {{STUDENT_CODE}}:\n    print("Even")\nelse:\n    print("Odd")',
    placeholder: 'num % 2 == 0',
    require: ['OP_MOD', 'COMPARISON'],
    solution: 'num % 2 == 0',
    explanation: 'A number is even when dividing it by 2 leaves no remainder. `num % 2` gives that remainder, and comparing it to 0 with `==` turns it into the True/False value `if` needs.',
    hints: [
      'Evenness is about the remainder after dividing by 2.',
      'The remainder operator is `%`.',
      'Compare the remainder to zero with `==`.',
    ],
    tests: [
      { setup: 'num = 4', expected: 'AUTO' },
      { setup: 'num = 7', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'num = 0', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'num = -3', expected: 'AUTO' },
    ],
    tags: ['conditions', 'modulo'],
  },
  {
    qid: 'PY-CONDIT-0002',
    language: 'python', topic: 'conditions', subtopic: 'if', difficulty: 'Easy',
    type: 'COMPLETE_CONDITION',
    title: 'Write the condition: is a greater than b?',
    statement: 'Write the condition that checks whether `a` is greater than `b`.',
    starter: '{{SETUP_CODE}}\n\nif {{STUDENT_CODE}}:\n    print("a wins")\nelse:\n    print("b wins")',
    placeholder: 'a > b',
    require: ['COMPARISON', 'OP_GT'],
    solution: 'a > b',
    explanation: 'Comparison operators produce booleans directly, so `a > b` is already the True/False value `if` expects — no `== True` is needed.',
    hints: [
      'Comparison operators return True or False on their own.',
      'The "greater than" symbol is `>`.',
      'Write `a > b`.',
    ],
    tests: [
      { setup: 'a = 9\nb = 3', expected: 'AUTO' },
      { setup: 'a = 2\nb = 8', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'a = 5\nb = 5', expected: 'AUTO' },
    ],
    tags: ['conditions', 'comparison'],
  },
  {
    qid: 'PY-CONDIT-0003',
    language: 'python', topic: 'conditions', subtopic: 'if', difficulty: 'Easy',
    title: 'Write a complete if statement',
    statement: 'Print `Adult` when `age` is 18 or more. Print nothing otherwise.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'if ...:\n    print("Adult")',
    require: ['IF', 'PRINT'],
    forbid: ['IF_ELSE'],
    solution: 'if age >= 18:\n    print("Adult")',
    explanation: 'An `if` statement is a header line ending in a colon, followed by an indented body. The body only runs when the condition is True — with no `else`, nothing happens when it is False.',
    hints: [
      'The header line ends with a colon.',
      '"18 or more" is the `>=` operator.',
      'The body must be indented under the `if`.',
    ],
    tests: [
      { setup: 'age = 20', expected: 'AUTO' },
      { setup: 'age = 12', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'age = 18', expected: 'AUTO' },
    ],
    tags: ['conditions', 'if'],
  },
  {
    qid: 'PY-CONDIT-0004',
    language: 'python', topic: 'conditions', subtopic: 'if-else', difficulty: 'Easy',
    title: 'Write an if-else statement',
    statement: 'Print `Positive` when `number` is greater than 0, otherwise print `Not positive`.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'if ...:\n    ...\nelse:\n    ...',
    require: ['IF_ELSE', 'PRINT'],
    solution: 'if number > 0:\n    print("Positive")\nelse:\n    print("Not positive")',
    explanation: '`else` catches every case the `if` condition did not. Both `if` and `else` end with a colon, and both bodies are indented one level.',
    hints: [
      'You need two branches, so the `if` needs a partner.',
      '`else:` has no condition of its own.',
      'Both branches are indented under their header line.',
    ],
    tests: [
      { setup: 'number = 5', expected: 'AUTO' },
      { setup: 'number = -2', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'number = 0', expected: 'AUTO' },
    ],
    tags: ['conditions', 'if-else'],
  },
  {
    qid: 'PY-CONDIT-0005',
    language: 'python', topic: 'conditions', subtopic: 'elif', difficulty: 'Medium',
    title: 'Add an elif branch',
    statement: 'Complete the grading logic: print `A` for 90 and above, `B` for 80-89, and `C` for anything lower.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'if ...:\n    ...\nelif ...:\n    ...\nelse:\n    ...',
    require: ['ELIF', 'PRINT'],
    solution: 'if score >= 90:\n    print("A")\nelif score >= 80:\n    print("B")\nelse:\n    print("C")',
    explanation: '`elif` chains extra conditions onto an `if`. Python tests them top to bottom and runs only the first branch that matches, which is why `score >= 80` correctly means "80-89" once the 90+ case has already been handled.',
    hints: [
      'A middle branch needs its own condition.',
      'Python spells "else if" as one word: `elif`.',
      'Order matters — check the highest boundary first.',
    ],
    tests: [
      { setup: 'score = 95', expected: 'AUTO' },
      { setup: 'score = 84', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'score = 79', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'score = 90', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'score = 80', expected: 'AUTO' },
    ],
    tags: ['conditions', 'elif'],
  },
  {
    qid: 'PY-CONDIT-0006',
    language: 'python', topic: 'conditions', subtopic: 'nested-conditions', difficulty: 'Medium',
    title: 'Nest one condition inside another',
    statement: 'Inside the existing `if`, add a check that prints `Teen driver` when `age` is under 20 and `Adult driver` otherwise.',
    starter: '{{SETUP_CODE}}\n\nif has_licence:\n    {{STUDENT_CODE}}\nelse:\n    print("No licence")',
    placeholder: 'if ...:\n    ...\nelse:\n    ...',
    require: ['IF_ELSE'],
    solution: 'if age < 20:\n    print("Teen driver")\nelse:\n    print("Adult driver")',
    explanation: 'A nested condition is just an `if` inside another branch, indented one level further. The inner test only runs when the outer condition already held.',
    hints: [
      'Your fragment goes inside the outer `if` body.',
      'It is a normal if-else — the editor handles the extra indentation.',
      'Check `age < 20` first.',
    ],
    tests: [
      { setup: 'has_licence = True\nage = 17', expected: 'AUTO' },
      { setup: 'has_licence = True\nage = 34', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'has_licence = False\nage = 17', expected: 'AUTO' },
    ],
    tags: ['conditions', 'nested'],
  },
  {
    qid: 'PY-CONDIT-0007',
    language: 'python', topic: 'conditions', subtopic: 'conditional-expressions', difficulty: 'Medium',
    title: 'Use a conditional expression',
    statement: 'Set `label` to `"even"` when `num` is even and `"odd"` otherwise, using a single-line conditional expression.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(label)',
    placeholder: 'label = ... if ... else ...',
    require: ['TERNARY'],
    forbid: ['IF'],
    config: { constructMessage: 'This question is practising the conditional expression — write it as one line with `if` and `else` inside the assignment, not as an if statement.' },
    solution: 'label = "even" if num % 2 == 0 else "odd"',
    explanation: 'A conditional expression evaluates to a value: `value_if_true if condition else value_if_false`. Unlike an `if` statement it can sit directly on the right of an assignment.',
    hints: [
      'Python has a one-line form of if-else that produces a value.',
      'The value comes first, then `if`, then the condition, then `else`.',
      'The shape is `A if condition else B`.',
    ],
    tests: [
      { setup: 'num = 8', expected: 'AUTO' },
      { setup: 'num = 3', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'num = 0', expected: 'AUTO' },
    ],
    tags: ['conditions', 'ternary'],
  },
  {
    qid: 'PY-CONDIT-0008',
    language: 'python', topic: 'conditions', subtopic: 'if', difficulty: 'Medium',
    type: 'COMPLETE_CONDITION',
    title: 'Combine two conditions with and',
    statement: 'Write the condition that is true only when `age` is at least 18 **and** `has_id` is true.',
    starter: '{{SETUP_CODE}}\n\nif {{STUDENT_CODE}}:\n    print("Allowed")\nelse:\n    print("Denied")',
    placeholder: 'age >= 18 and has_id',
    require: ['OP_AND'],
    solution: 'age >= 18 and has_id',
    explanation: '`and` is true only when both sides are true. A boolean variable like `has_id` needs no comparison — writing `has_id == True` works but is redundant.',
    hints: [
      'Both requirements must hold at the same time.',
      'Python spells the operator as a word, not `&&`.',
      'Write `age >= 18 and has_id`.',
    ],
    tests: [
      { setup: 'age = 25\nhas_id = True', expected: 'AUTO' },
      { setup: 'age = 25\nhas_id = False', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'age = 15\nhas_id = True', expected: 'AUTO' },
    ],
    tags: ['conditions', 'boolean-operators'],
  },
  {
    qid: 'PY-CONDIT-0009',
    language: 'python', topic: 'conditions', subtopic: 'if', difficulty: 'Medium',
    type: 'COMPLETE_CONDITION',
    title: 'Check membership in a list',
    statement: 'Write the condition that checks whether `colour` is one of the values in the `allowed` list.',
    starter: '{{SETUP_CODE}}\n\nif {{STUDENT_CODE}}:\n    print("Available")\nelse:\n    print("Out of stock")',
    placeholder: 'colour in allowed',
    require: ['OP_IN'],
    forbid: ['FOR_LOOP', 'WHILE_LOOP'],
    solution: 'colour in allowed',
    explanation: 'The `in` operator tests membership and returns a boolean, so no loop is needed to search the list yourself.',
    hints: [
      'Python has a dedicated membership operator.',
      'It is a two-letter word.',
      'Write `colour in allowed`.',
    ],
    tests: [
      { setup: 'allowed = ["red", "green", "blue"]\ncolour = "green"', expected: 'AUTO' },
      { setup: 'allowed = ["red", "green", "blue"]\ncolour = "pink"', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'allowed = []\ncolour = "red"', expected: 'AUTO' },
    ],
    tags: ['conditions', 'membership'],
  },
  {
    qid: 'PY-CONDIT-0010',
    language: 'python', topic: 'conditions', subtopic: 'if', difficulty: 'Medium',
    type: 'COMPLETE_CONDITION',
    title: 'Check whether a list is empty',
    statement: 'Write the condition that is true when the list `items` has no elements.',
    starter: '{{SETUP_CODE}}\n\nif {{STUDENT_CODE}}:\n    print("Empty")\nelse:\n    print("Has items")',
    placeholder: 'len(items) == 0',
    solution: 'len(items) == 0',
    altSolutions: ['not items', 'items == []'],
    explanation: 'Any of `len(items) == 0`, `not items` or `items == []` works. Empty sequences are falsy in Python, which is why `not items` reads as "items is empty".',
    hints: [
      '`len()` tells you how many elements a list holds.',
      'An empty list has length zero.',
      'Compare the length to 0, or use the fact that empty lists are falsy.',
    ],
    tests: [
      { setup: 'items = []', expected: 'AUTO' },
      { setup: 'items = [1, 2]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'items = [0]', expected: 'AUTO' },
    ],
    tags: ['conditions', 'truthiness'],
  },
];
