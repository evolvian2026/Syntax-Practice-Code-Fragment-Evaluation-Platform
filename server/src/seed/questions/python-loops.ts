import type { SeedQuestion } from './types.js';

/** Python — Loops (15). */
export const PYTHON_LOOPS: SeedQuestion[] = [
  {
    qid: 'PY-LOOPS-0001',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'Print every number in a list',
    statement: 'Write a `for` loop that prints every number in `numbers`, one per line.',
    instructions: 'The list already exists. Write only the loop.',
    objective: 'Iterate over a list with a for loop.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint("Done")',
    placeholder: 'for n in numbers:\n    print(n)',
    require: ['FOR_LOOP', 'PRINT'],
    config: { constructMessage: 'Output is correct, but this question is practising the FOR loop — write the loop rather than a one-line print trick.' },
    solution: 'for n in numbers:\n    print(n)',
    altSolutions: ['for number in numbers:\n    print(number)'],
    explanation: 'A `for` loop walks through each item of a collection: `for item in collection:` followed by an indented body. The loop variable takes each value in turn, so `print(n)` runs once per element.',
    hints: [
      'Which loop is used to walk through every item of a list?',
      'The loop header starts with `for` and ends with a colon.',
      'Try `for item in numbers:` with an indented `print(item)` under it.',
    ],
    tests: [
      { setup: 'numbers = [1, 2, 3, 4, 5]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [42]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = []', expected: 'AUTO' },
    ],
    tags: ['loops', 'for'],
  },
  {
    qid: 'PY-LOOPS-0002',
    language: 'python', topic: 'loops', subtopic: 'range', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'Count from 1 to 5',
    statement: 'Write a `for` loop that prints the numbers 1 to 5, one per line, using `range()`.',
    starter: '{{STUDENT_CODE}}\n\nprint("Done")',
    placeholder: 'for i in range(...):\n    print(i)',
    require: ['FOR_LOOP', 'RANGE', 'PRINT'],
    solution: 'for i in range(1, 6):\n    print(i)',
    explanation: '`range(start, stop)` produces numbers from `start` up to but *not including* `stop`, so counting to 5 needs `range(1, 6)`. This off-by-one rule is the most common range mistake.',
    hints: [
      '`range()` generates a sequence of numbers to loop over.',
      'With two arguments it is `range(start, stop)`.',
      'The stop value is excluded — to reach 5 you must write 6.',
    ],
    tests: [{ expected: 'AUTO' }],
    tags: ['loops', 'range'],
  },
  {
    qid: 'PY-LOOPS-0003',
    language: 'python', topic: 'loops', subtopic: 'range', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'Count in steps of 2',
    statement: 'Print the even numbers from 0 to 10 inclusive, one per line, using a single `range()` call.',
    starter: '{{STUDENT_CODE}}',
    placeholder: 'for i in range(...):\n    print(i)',
    require: ['FOR_LOOP', 'RANGE'],
    forbid: ['IF'],
    solution: 'for i in range(0, 11, 2):\n    print(i)',
    explanation: '`range(start, stop, step)` takes a third argument: the step size. `range(0, 11, 2)` yields 0, 2, 4, 6, 8, 10 — no `if` filter needed.',
    hints: [
      '`range()` accepts a third argument.',
      'That argument is the step between values.',
      'Remember the stop value is excluded, so use 11 to include 10.',
    ],
    tags: ['loops', 'range', 'step'],
  },
  {
    qid: 'PY-LOOPS-0004',
    language: 'python', topic: 'loops', subtopic: 'while', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'Count down with while',
    statement: 'Using a `while` loop, print `count` and decrease it by 1 until it reaches 0.',
    starter: 'count = 5\n\n{{STUDENT_CODE}}\n\nprint("Liftoff")',
    placeholder: 'while ...:\n    ...',
    require: ['WHILE_LOOP'],
    forbid: ['FOR_LOOP'],
    solution: 'while count > 0:\n    print(count)\n    count -= 1',
    explanation: 'A `while` loop repeats as long as its condition is true. The body must change something the condition depends on — here `count -= 1` — otherwise the loop never ends.',
    hints: [
      'A `while` loop needs a condition that eventually becomes false.',
      'Print first, then decrease the counter inside the loop body.',
      'Without `count -= 1` the loop would run forever.',
    ],
    tags: ['loops', 'while'],
  },
  {
    qid: 'PY-LOOPS-0005',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Medium',
    title: 'Sum a list with a loop',
    statement: 'Add every number in `numbers` to the variable `total` using a loop.',
    starter: '{{SETUP_CODE}}\ntotal = 0\n\n{{STUDENT_CODE}}\n\nprint(total)',
    placeholder: 'for n in numbers:\n    total += n',
    require: ['FOR_LOOP', 'AUG_ASSIGN'],
    forbid: ['CALL:sum'],
    config: { constructMessage: 'The result may be right, but this question is practising accumulation in a loop — build the total yourself instead of calling sum().' },
    solution: 'for n in numbers:\n    total += n',
    explanation: 'The accumulator pattern: start at a neutral value, then add each item inside the loop. `total += n` is shorthand for `total = total + n` and is the building block behind `sum()`.',
    hints: [
      'Keep a running total that grows on each pass.',
      'Add the current item to `total` inside the loop.',
      'Use `total += n` in the loop body.',
    ],
    tests: [
      { setup: 'numbers = [1, 2, 3, 4]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = []', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [-5, 5, 10]', expected: 'AUTO' },
    ],
    tags: ['loops', 'accumulator'],
  },
  {
    qid: 'PY-LOOPS-0006',
    language: 'python', topic: 'loops', subtopic: 'break', difficulty: 'Medium',
    title: 'Stop the loop early with break',
    statement: 'Print each number in `numbers`, but stop the loop completely as soon as you reach a number greater than 10.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint("Finished")',
    placeholder: 'for n in numbers:\n    if ...:\n        break\n    print(n)',
    require: ['FOR_LOOP', 'BREAK', 'IF'],
    solution: 'for n in numbers:\n    if n > 10:\n        break\n    print(n)',
    explanation: '`break` exits the innermost loop immediately, skipping the remaining items. Putting the check *before* the print is what keeps the large number from being printed.',
    hints: [
      'There is a keyword that leaves a loop immediately.',
      'The check has to happen before the print.',
      'Use `if n > 10:` then `break` on the next indented line.',
    ],
    tests: [
      { setup: 'numbers = [1, 5, 12, 3]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [20, 1]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [1, 2, 3]', expected: 'AUTO' },
    ],
    tags: ['loops', 'break'],
  },
  {
    qid: 'PY-LOOPS-0007',
    language: 'python', topic: 'loops', subtopic: 'continue', difficulty: 'Medium',
    title: 'Skip an item with continue',
    statement: 'Print every number in `numbers` except the odd ones, using `continue` to skip them.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'for n in numbers:\n    if ...:\n        continue\n    print(n)',
    require: ['FOR_LOOP', 'CONTINUE'],
    solution: 'for n in numbers:\n    if n % 2 != 0:\n        continue\n    print(n)',
    explanation: '`continue` abandons the current iteration and jumps to the next one, leaving the loop itself running. It is the "skip this one" counterpart to `break`.',
    hints: [
      'One keyword skips the rest of the current pass but keeps looping.',
      'Odd numbers have a remainder of 1 when divided by 2.',
      'Use `if n % 2 != 0:` then `continue`.',
    ],
    tests: [
      { setup: 'numbers = [1, 2, 3, 4, 5, 6]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [1, 3, 5]', expected: 'AUTO' },
    ],
    tags: ['loops', 'continue'],
  },
  {
    qid: 'PY-LOOPS-0008',
    language: 'python', topic: 'loops', subtopic: 'nested-loops', difficulty: 'Hard',
    title: 'Nested loops: multiplication grid',
    statement: 'Print every product `i * j` for `i` in 1..3 and `j` in 1..3, one per line, using two nested loops.',
    starter: '{{STUDENT_CODE}}',
    placeholder: 'for i in range(...):\n    for j in range(...):\n        print(...)',
    require: ['NESTED_LOOP', 'RANGE'],
    solution: 'for i in range(1, 4):\n    for j in range(1, 4):\n        print(i * j)',
    explanation: 'The inner loop runs completely for every single pass of the outer loop, giving 3 x 3 = 9 lines. Each level of nesting adds one level of indentation.',
    hints: [
      'You need one loop inside the body of another.',
      'The inner loop restarts on every outer pass.',
      'Indent the inner loop one level, and its body two levels.',
    ],
    tags: ['loops', 'nested'],
  },
  {
    qid: 'PY-LOOPS-0009',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'Loop over a string',
    statement: 'Print each character of `word` on its own line.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'for ch in word:\n    print(ch)',
    require: ['FOR_LOOP', 'PRINT'],
    solution: 'for ch in word:\n    print(ch)',
    explanation: 'Strings are sequences, so a `for` loop walks them character by character — no indexing required.',
    hints: [
      'Strings can be looped over just like lists.',
      'Each pass gives you one character.',
      'Write `for ch in word:` with `print(ch)` inside.',
    ],
    tests: [
      { setup: 'word = "cat"', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'word = "a"', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'word = ""', expected: 'AUTO' },
    ],
    tags: ['loops', 'strings'],
  },
  {
    qid: 'PY-LOOPS-0010',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Medium',
    title: 'Loop with position numbers',
    statement: 'Print each fruit with its position, starting at 1, in the form `1. apple`.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'for i, fruit in enumerate(...):\n    print(...)',
    require: ['FOR_LOOP'],
    solution: 'for i, fruit in enumerate(fruits, start=1):\n    print(f"{i}. {fruit}")',
    altSolutions: ['for i in range(len(fruits)):\n    print(f"{i + 1}. {fruits[i]}")'],
    explanation: '`enumerate()` yields `(index, item)` pairs, and its `start` argument shifts the counting base. Unpacking both values in the loop header is cleaner than indexing manually.',
    hints: [
      'There is a built-in that pairs each item with its index.',
      '`enumerate()` accepts a `start` argument.',
      'Unpack two names in the loop header: `for i, item in ...`.',
    ],
    tests: [
      { setup: 'fruits = ["apple", "banana"]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'fruits = ["one", "two", "three"]', expected: 'AUTO' },
    ],
    tags: ['loops', 'enumerate'],
  },
  {
    qid: 'PY-LOOPS-0011',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Medium',
    title: 'Loop over a dictionary',
    statement: 'Print each key and value of `prices` in the form `apple: 30`.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'for key, value in prices.items():\n    print(...)',
    require: ['FOR_LOOP', 'METHOD:items'],
    solution: 'for name, price in prices.items():\n    print(f"{name}: {price}")',
    explanation: '`.items()` yields `(key, value)` pairs, which the loop header unpacks into two names. Looping over the dictionary directly would give you only the keys.',
    hints: [
      'Looping a dictionary directly gives keys only.',
      'There is a method that yields key/value pairs.',
      'Use `prices.items()` and unpack two names.',
    ],
    tests: [
      { setup: 'prices = {"apple": 30, "banana": 10}', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'prices = {"solo": 1}', expected: 'AUTO' },
    ],
    tags: ['loops', 'dictionaries'],
  },
  {
    qid: 'PY-LOOPS-0012',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Medium',
    title: 'Filter inside a loop',
    statement: 'Print only the numbers in `numbers` that are greater than 10.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'for n in numbers:\n    if ...:\n        print(n)',
    require: ['FOR_LOOP', 'IF'],
    solution: 'for n in numbers:\n    if n > 10:\n        print(n)',
    explanation: 'Combining a loop with an `if` filters as you go. The `if` sits inside the loop body, so it is evaluated once per item.',
    hints: [
      'Every item is visited, but only some are printed.',
      'Put an `if` inside the loop body.',
      'The `print` is indented under the `if`.',
    ],
    tests: [
      { setup: 'numbers = [5, 12, 30, 7]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [1, 2]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [11]', expected: 'AUTO' },
    ],
    tags: ['loops', 'filtering'],
  },
  {
    qid: 'PY-LOOPS-0013',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Medium',
    title: 'Build a list inside a loop',
    statement: 'Fill the list `doubled` with every number in `numbers` multiplied by 2, using a loop and `append()`.',
    starter: '{{SETUP_CODE}}\ndoubled = []\n\n{{STUDENT_CODE}}\n\nprint(doubled)',
    placeholder: 'for n in numbers:\n    doubled.append(...)',
    require: ['FOR_LOOP', 'METHOD:append'],
    forbid: ['LIST_COMPREHENSION'],
    config: { constructMessage: 'This question is practising the loop-and-append pattern — save the comprehension for the list-comprehension question.' },
    solution: 'for n in numbers:\n    doubled.append(n * 2)',
    explanation: 'Start with an empty list, then append one result per iteration. This is the long form of a list comprehension and shows exactly what the comprehension does under the hood.',
    hints: [
      'The empty list is already created for you.',
      'Add one item per pass with `.append()`.',
      'Append `n * 2` inside the loop.',
    ],
    tests: [
      { setup: 'numbers = [1, 2, 3]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = []', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [-1, 0]', expected: 'AUTO' },
    ],
    tags: ['loops', 'append'],
  },
  {
    qid: 'PY-LOOPS-0014',
    language: 'python', topic: 'loops', subtopic: 'while', difficulty: 'Hard',
    title: 'While loop with an accumulator',
    statement: 'Using a `while` loop, keep doubling `value` until it is 100 or more, counting the steps in `steps`.',
    starter: 'value = 3\nsteps = 0\n\n{{STUDENT_CODE}}\n\nprint(value, steps)',
    placeholder: 'while ...:\n    ...',
    require: ['WHILE_LOOP'],
    forbid: ['FOR_LOOP'],
    solution: 'while value < 100:\n    value *= 2\n    steps += 1',
    explanation: 'A `while` loop is the right choice when you do not know the number of iterations in advance — only the stopping condition. Both the value and the counter are updated inside the body.',
    hints: [
      'The condition describes when to keep going, not when to stop.',
      'Double the value and increase the counter in the same body.',
      'Use `while value < 100:`.',
    ],
    tags: ['loops', 'while', 'accumulator'],
  },
  {
    qid: 'PY-LOOPS-0015',
    language: 'python', topic: 'loops', subtopic: 'nested-loops', difficulty: 'Hard',
    title: 'Nested loop over a grid',
    statement: 'Print every value in the 2D list `grid`, one per line, row by row.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'for row in grid:\n    for cell in row:\n        print(cell)',
    require: ['NESTED_LOOP'],
    solution: 'for row in grid:\n    for cell in row:\n        print(cell)',
    explanation: 'The outer loop walks the rows (each of which is itself a list), and the inner loop walks the cells of the current row. This is the standard way to visit every element of a 2D structure.',
    hints: [
      'Each item of `grid` is itself a list.',
      'Loop over the rows first, then over each row.',
      'The inner loop body is indented two levels.',
    ],
    tests: [
      { setup: 'grid = [[1, 2], [3, 4]]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'grid = [[7]]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'grid = [[1, 2, 3], [], [4]]', expected: 'AUTO' },
    ],
    tags: ['loops', 'nested', '2d'],
  },
];
