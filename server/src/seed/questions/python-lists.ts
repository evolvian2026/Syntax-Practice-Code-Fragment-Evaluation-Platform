import type { SeedQuestion } from './types.js';

/** Python — Lists (10). */
export const PYTHON_LISTS: SeedQuestion[] = [
  {
    qid: 'PY-LISTS-0001',
    language: 'python', topic: 'lists', subtopic: 'list-creation', difficulty: 'Easy',
    type: 'IDENTIFY_SYNTAX', evaluation: 'SYNTAX',
    title: 'Create a list',
    statement: 'Write a Python list containing the numbers 5, 10 and 15 — just the list expression itself.',
    instructions: 'Only the expression is graded, so spacing does not matter: `[5,10,15]` and `[5, 10, 15]` are both accepted.',
    objective: 'Write a list literal.',
    starter: 'numbers = {{STUDENT_CODE}}\n\nprint(numbers)',
    placeholder: '[...]',
    require: ['LIST_LITERAL'],
    solution: '[5, 10, 15]',
    altSolutions: ['[5,10,15]'],
    explanation: 'A list literal is written with square brackets and comma-separated values: `[5, 10, 15]`. Because this question is graded on the parsed expression rather than the raw text, whitespace between the items is irrelevant.',
    hints: [
      'Lists use one particular kind of bracket.',
      'Square brackets hold the items, separated by commas.',
      'The shape is `[item1, item2, item3]`.',
    ],
    tests: [],
    tags: ['lists', 'literals'],
  },
  {
    qid: 'PY-LISTS-0002',
    language: 'python', topic: 'lists', subtopic: 'indexing', difficulty: 'Easy',
    title: 'Read the first item',
    statement: 'Store the first item of `fruits` in a variable called `first`.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(first)',
    placeholder: 'first = ...',
    require: ['SUBSCRIPT'],
    solution: 'first = fruits[0]',
    explanation: 'List positions start at 0, so the first item is `fruits[0]`. Using `fruits[1]` would give you the second item.',
    hints: [
      'Items are read with square brackets after the list name.',
      'Python counts positions from zero, not one.',
      'Write `first = fruits[0]`.',
    ],
    tests: [
      { setup: 'fruits = ["apple", "banana", "cherry"]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'fruits = ["kiwi"]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'fruits = ["mango", "pear"]', expected: 'AUTO' },
    ],
    tags: ['lists', 'indexing'],
  },
  {
    qid: 'PY-LISTS-0003',
    language: 'python', topic: 'lists', subtopic: 'indexing', difficulty: 'Easy',
    title: 'Read the last item',
    statement: 'Store the last item of `fruits` in `last`, whatever the length of the list is.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(last)',
    placeholder: 'last = ...',
    require: ['SUBSCRIPT'],
    solution: 'last = fruits[-1]',
    altSolutions: ['last = fruits[len(fruits) - 1]'],
    explanation: 'Negative indexes count from the end, so `fruits[-1]` is always the last item regardless of length. Hard-coding `fruits[2]` would break as soon as the list changed size.',
    hints: [
      'You do not know the length, so a fixed number will not do.',
      'Python indexes can be negative, counting backwards from the end.',
      'Write `last = fruits[-1]`.',
    ],
    tests: [
      { setup: 'fruits = ["apple", "banana", "cherry"]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'fruits = ["only"]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'fruits = ["a", "b", "c", "d", "e"]', expected: 'AUTO' },
    ],
    tags: ['lists', 'indexing', 'negative-index'],
  },
  {
    qid: 'PY-LISTS-0004',
    language: 'python', topic: 'lists', subtopic: 'slicing', difficulty: 'Medium',
    title: 'Slice the first three items',
    statement: 'Store the first three items of `numbers` in a new list called `top_three`.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(top_three)',
    placeholder: 'top_three = ...',
    require: ['SLICING'],
    forbid: ['FOR_LOOP', 'WHILE_LOOP'],
    solution: 'top_three = numbers[0:3]',
    altSolutions: ['top_three = numbers[:3]'],
    explanation: 'A slice `numbers[start:stop]` returns a new list from `start` up to but not including `stop`, so `[:3]` gives positions 0, 1 and 2. Leaving the start empty means "from the beginning".',
    hints: [
      'Slicing uses a colon inside the square brackets.',
      'The end position is excluded from the result.',
      'The shape is `list[:3]` or `list[0:3]`.',
    ],
    tests: [
      { setup: 'numbers = [10, 20, 30, 40, 50]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [1, 2]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [9, 8, 7, 6]', expected: 'AUTO' },
    ],
    tags: ['lists', 'slicing'],
  },
  {
    qid: 'PY-LISTS-0005',
    language: 'python', topic: 'lists', subtopic: 'append', difficulty: 'Easy',
    type: 'COMPLETE_STATEMENT',
    title: 'Append an item',
    statement: 'Complete the statement so that the number 4 is added to the end of the list.',
    instructions: 'The statement already starts with `numbers.` — write only what comes after the dot.',
    starter: 'numbers = [1, 2, 3]\n\nnumbers.{{STUDENT_CODE}}\n\nprint(numbers)',
    placeholder: 'append(4)',
    require: ['METHOD:append'],
    solution: 'append(4)',
    explanation: '`append()` adds a single item to the end of a list, modifying the list in place. It always adds exactly one element — `numbers.append([4])` would add a list as one item.',
    hints: [
      'There is a list method for adding one item at the end.',
      'Its name is six letters and starts with "app".',
      'Write `append(4)`.',
    ],
    tags: ['lists', 'append', 'methods'],
  },
  {
    qid: 'PY-LISTS-0006',
    language: 'python', topic: 'lists', subtopic: 'extend', difficulty: 'Medium',
    title: 'Add several items at once',
    statement: 'Add every item of `extra` to the end of `numbers`, so that `numbers` grows by three elements — not by one.',
    starter: 'numbers = [1, 2, 3]\nextra = [4, 5, 6]\n\n{{STUDENT_CODE}}\n\nprint(numbers)',
    placeholder: 'numbers.extend(...)',
    require: ['ANY:METHOD:extend|AUG_ASSIGN'],
    forbid: ['METHOD:append'],
    solution: 'numbers.extend(extra)',
    altSolutions: ['numbers += extra'],
    explanation: '`extend()` unpacks the iterable and adds each of its items separately, so the list grows by three. `append(extra)` would instead nest the whole list as a single fourth element.',
    hints: [
      '`append` would add the other list as one single item.',
      'There is a different method that merges the items in.',
      'Write `numbers.extend(extra)`.',
    ],
    tags: ['lists', 'extend', 'methods'],
  },
  {
    qid: 'PY-LISTS-0007',
    language: 'python', topic: 'lists', subtopic: 'insert', difficulty: 'Medium',
    title: 'Insert at a position',
    statement: 'Insert the value `99` at the very beginning of `numbers`, keeping every existing item.',
    starter: 'numbers = [1, 2, 3]\n\n{{STUDENT_CODE}}\n\nprint(numbers)',
    placeholder: 'numbers.insert(...)',
    require: ['METHOD:insert'],
    solution: 'numbers.insert(0, 99)',
    explanation: '`insert(index, value)` puts a value at a specific position and shifts everything after it one place to the right. Index 0 means "before everything else".',
    hints: [
      'There is a method that takes both a position and a value.',
      'The position comes first, the value second.',
      'Write `numbers.insert(0, 99)`.',
    ],
    tags: ['lists', 'insert', 'methods'],
  },
  {
    qid: 'PY-LISTS-0008',
    language: 'python', topic: 'lists', subtopic: 'remove', difficulty: 'Easy',
    title: 'Remove a value',
    statement: 'Remove the value `"banana"` from `fruits`. Remove it by value, not by position.',
    starter: 'fruits = ["apple", "banana", "cherry"]\n\n{{STUDENT_CODE}}\n\nprint(fruits)',
    placeholder: 'fruits.remove(...)',
    require: ['METHOD:remove'],
    forbid: ['METHOD:pop', 'DELETE'],
    solution: 'fruits.remove("banana")',
    explanation: '`remove(value)` deletes the first occurrence of that value. `pop(index)` is the positional counterpart — use `remove` when you know *what* to delete and `pop` when you know *where*.',
    hints: [
      'There is a method that takes the value you want gone.',
      'It is different from `pop`, which takes a position.',
      'Write `fruits.remove("banana")`.',
    ],
    tags: ['lists', 'remove', 'methods'],
  },
  {
    qid: 'PY-LISTS-0009',
    language: 'python', topic: 'lists', subtopic: 'sort', difficulty: 'Medium',
    title: 'Sort a list in place',
    statement: 'Sort `numbers` from largest to smallest, modifying the list itself.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(numbers)',
    placeholder: 'numbers.sort(...)',
    require: ['METHOD:sort', 'KEYWORD_ARGUMENT'],
    solution: 'numbers.sort(reverse=True)',
    explanation: '`sort()` reorders the list in place; the keyword argument `reverse=True` flips it to descending. `sorted(numbers, reverse=True)` would instead return a new list and leave the original untouched.',
    hints: [
      'The list method that sorts in place is `sort()`.',
      'It accepts a keyword argument to reverse the order.',
      'Write `numbers.sort(reverse=True)`.',
    ],
    tests: [
      { setup: 'numbers = [3, 1, 4, 1, 5]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [10, 2, 33]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [7]', expected: 'AUTO' },
    ],
    tags: ['lists', 'sort', 'methods'],
  },
  {
    qid: 'PY-LISTS-0010',
    language: 'python', topic: 'lists', subtopic: 'list-comprehension', difficulty: 'Medium',
    title: 'Build a list with a comprehension',
    statement: 'Create `squares`, a list of every number in `numbers` squared, using a list comprehension.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(squares)',
    placeholder: 'squares = [...]',
    require: ['LIST_COMPREHENSION'],
    forbid: ['METHOD:append'],
    config: { constructMessage: 'This question is practising list comprehensions — build the list in one expression rather than with a loop and append().' },
    solution: 'squares = [n * n for n in numbers]',
    altSolutions: ['squares = [n ** 2 for n in numbers]'],
    explanation: 'A list comprehension builds a list in a single expression: `[expression for item in iterable]`. It replaces the three-line pattern of creating an empty list, looping, and appending.',
    hints: [
      'A comprehension is written inside square brackets.',
      'It contains an expression followed by a `for` clause.',
      'The shape is `[expression for item in collection]`.',
    ],
    tests: [
      { setup: 'numbers = [1, 2, 3, 4]', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = []', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'numbers = [-3, 5]', expected: 'AUTO' },
    ],
    tags: ['lists', 'comprehension'],
  },
];
