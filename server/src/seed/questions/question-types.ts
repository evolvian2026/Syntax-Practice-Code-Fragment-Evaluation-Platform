import type { SeedQuestion } from './types.js';

/**
 * §11 — coverage for the remaining question types (fix-the-syntax, predict
 * output, choose-and-write) plus the Python collection/OOP/exception topics.
 */
export const QUESTION_TYPES: SeedQuestion[] = [
  // --------------------------------------------- Type 7: Fix the syntax
  {
    qid: 'PY-LOOPS-0016',
    language: 'python', topic: 'loops', subtopic: 'for', difficulty: 'Easy',
    type: 'FIX_SYNTAX',
    title: 'Fix the broken for loop',
    statement: 'This loop header is missing something. Rewrite the header line so the code runs.',
    instructions: 'The broken line has been loaded into the editor. Fix only that line.',
    starter: 'numbers = [1, 2, 3]\n\n{{STUDENT_CODE}}\n    print(n)',
    prefill: 'for n in numbers',
    require: ['FOR_LOOP'],
    solution: 'for n in numbers:',
    explanation: 'Every Python block header — `for`, `while`, `if`, `def`, `class` — ends with a colon. Leaving it off produces `SyntaxError: expected \':\'`.',
    hints: [
      'Python block headers all end with the same punctuation mark.',
      'The error message names the missing character.',
      'Add a colon at the end of the line.',
    ],
    tests: [{ expected: 'AUTO' }],
    tags: ['loops', 'fix-syntax'],
  },
  {
    qid: 'PY-FUNCTI-0011',
    language: 'python', topic: 'functions', subtopic: 'function-definition', difficulty: 'Medium',
    type: 'FIX_SYNTAX',
    title: 'Fix the function definition',
    statement: 'This function definition has two problems. Rewrite the header so `add(2, 3)` works.',
    starter: '{{STUDENT_CODE}}\n    return a + b\n\nprint(add(2, 3))',
    prefill: 'def add(a b)',
    require: ['FUNCTION_DEF'],
    solution: 'def add(a, b):',
    explanation: 'Parameters must be separated by commas, and the `def` header ends with a colon. Both are required — Python reports only the first error it hits.',
    hints: [
      'Look at how the two parameters are separated.',
      'Then look at the end of the line.',
      'Parameters need a comma; the header needs a colon.',
    ],
    tags: ['functions', 'fix-syntax'],
  },
  {
    qid: 'MYS-WHERE-0006',
    language: 'mysql', topic: 'where', subtopic: 'comparison', difficulty: 'Medium',
    type: 'FIX_SYNTAX', evaluation: 'SQL_RESULT', dataset: 'company',
    title: 'Fix the WHERE condition',
    statement: 'This condition uses the wrong equality operator for SQL. Fix it so employees in department 2 are returned.',
    starter: 'SELECT name, department_id\nFROM Employees\nWHERE {{STUDENT_CODE}};',
    prefill: 'department_id == 2',
    solution: 'department_id = 2',
    explanation: 'SQL compares with a single `=`. The doubled `==` belongs to languages like Python, C and Java; most SQL engines reject it outright.',
    hints: [
      'SQL comparison does not use the same operator as Python.',
      'One character too many.',
      'Use a single `=`.',
    ],
    tags: ['sql', 'fix-syntax'],
  },

  // ------------------------------------------- Type 8: Predict the output
  {
    qid: 'PY-LOOPS-0017',
    language: 'python', topic: 'loops', subtopic: 'range', difficulty: 'Medium',
    type: 'PREDICT_OUTPUT', evaluation: 'TEXT',
    title: 'Predict the output: range',
    statement: 'What exactly does this code print? Type the output, one value per line.',
    instructions: 'Do not run anything — read the code and type what it prints.',
    starter: 'for i in range(1, 4):\n    print(i * 2)\n\n{{STUDENT_CODE}}',
    placeholder: 'Type the expected output here',
    config: { acceptedText: ['2\n4\n6'] },
    solution: '2\n4\n6',
    explanation: '`range(1, 4)` yields 1, 2 and 3 — the stop value is excluded. Each is doubled, giving 2, 4 and 6 on separate lines.',
    hints: [
      'Work out which values `range(1, 4)` produces.',
      'The stop value is not included.',
      'Then apply `* 2` to each of them.',
    ],
    tests: [],
    tags: ['loops', 'predict-output'],
  },
  {
    qid: 'PY-LISTS-0011',
    language: 'python', topic: 'lists', subtopic: 'slicing', difficulty: 'Medium',
    type: 'PREDICT_OUTPUT', evaluation: 'TEXT',
    title: 'Predict the output: slicing',
    statement: 'What does this code print?',
    starter: 'nums = [10, 20, 30, 40, 50]\nprint(nums[1:4])\n\n{{STUDENT_CODE}}',
    placeholder: 'Type the expected output here',
    config: { acceptedText: ['[20, 30, 40]', '[20,30,40]'] },
    solution: '[20, 30, 40]',
    explanation: '`nums[1:4]` starts at index 1 (the value 20) and stops *before* index 4, so index 4 (50) is left out. The result is `[20, 30, 40]`.',
    hints: [
      'Slicing starts at the first index and stops before the second.',
      'Index 1 is the second element.',
      'Three elements come back.',
    ],
    tests: [],
    tags: ['lists', 'predict-output'],
  },

  // ------------------------------------------ Type 6: Choose and write
  {
    qid: 'PY-LISTS-0012',
    language: 'python', topic: 'lists', subtopic: 'append', difficulty: 'Medium',
    type: 'CHOOSE_AND_WRITE',
    title: 'Choose the right method, then write it',
    statement: 'Which method adds a single item to the end of a list? Pick it, then write the full statement that adds `4` to `numbers`.',
    instructions: 'Select an option to check your understanding, then write the complete statement in the editor.',
    starter: 'numbers = [1, 2, 3]\n\n{{STUDENT_CODE}}\n\nprint(numbers)',
    options: ['append()', 'extend()', 'insert()', 'add()'],
    placeholder: 'numbers.append(4)',
    require: ['METHOD:append'],
    solution: 'numbers.append(4)',
    explanation: '`append()` adds one item to the end. `extend()` merges an iterable, `insert()` needs a position, and `add()` belongs to sets, not lists.',
    hints: [
      'Three of the four options are real list methods.',
      'Only one adds exactly one item at the end.',
      'Write `numbers.append(4)`.',
    ],
    tags: ['lists', 'choose-and-write'],
  },

  // ------------------------------------------------ Python: dictionaries
  {
    qid: 'PY-DICTIO-0001',
    language: 'python', topic: 'dictionaries', subtopic: 'dictionary-creation', difficulty: 'Easy',
    type: 'IDENTIFY_SYNTAX', evaluation: 'SYNTAX',
    title: 'Create a dictionary',
    statement: 'Write a dictionary mapping `"a"` to 1 and `"b"` to 2 — just the literal.',
    starter: 'data = {{STUDENT_CODE}}\n\nprint(data)',
    placeholder: '{...}',
    require: ['DICT_LITERAL'],
    solution: '{"a": 1, "b": 2}',
    altSolutions: ["{'a': 1, 'b': 2}"],
    explanation: 'A dictionary literal uses curly braces with `key: value` pairs separated by commas. Note that `{}` is an empty dictionary, not an empty set.',
    hints: [
      'Dictionaries use curly braces.',
      'Each entry is a key, a colon, then a value.',
      'The shape is `{"key": value, ...}`.',
    ],
    tests: [],
    tags: ['dictionaries'],
  },
  {
    qid: 'PY-DICTIO-0002',
    language: 'python', topic: 'dictionaries', subtopic: 'accessing-values', difficulty: 'Easy',
    title: 'Read a value safely',
    statement: 'Read the value for the key `"city"` from `person`, falling back to `"Unknown"` when the key is missing.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(city)',
    placeholder: 'city = person.get(...)',
    require: ['METHOD:get'],
    solution: 'city = person.get("city", "Unknown")',
    explanation: '`person["city"]` raises `KeyError` when the key is missing. `.get(key, default)` returns the default instead, which is what makes it "safe".',
    hints: [
      'Square-bracket access raises an error on a missing key.',
      'There is a method that accepts a fallback value.',
      'Write `person.get("city", "Unknown")`.',
    ],
    tests: [
      { setup: 'person = {"name": "Ada", "city": "London"}', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'person = {"name": "Ada"}', expected: 'AUTO' },
    ],
    tags: ['dictionaries'],
  },
  {
    qid: 'PY-DICTIO-0003',
    language: 'python', topic: 'dictionaries', subtopic: 'adding-values', difficulty: 'Easy',
    title: 'Add a key to a dictionary',
    statement: 'Add the key `"role"` with the value `"admin"` to `person`.',
    starter: 'person = {"name": "Ada"}\n\n{{STUDENT_CODE}}\n\nprint(person)',
    placeholder: 'person["role"] = ...',
    require: ['SUBSCRIPT', 'ASSIGNMENT'],
    solution: 'person["role"] = "admin"',
    explanation: 'Assigning to a key that does not exist creates it; assigning to one that does replaces its value. There is no separate "add" operation for dictionaries.',
    hints: [
      'Use square brackets with the new key.',
      'Assign to it as if it already existed.',
      'Write `person["role"] = "admin"`.',
    ],
    tags: ['dictionaries'],
  },

  // ------------------------------------------------------ Python: tuples
  {
    qid: 'PY-TUPLES-0001',
    language: 'python', topic: 'tuples', subtopic: 'tuple-creation', difficulty: 'Easy',
    type: 'IDENTIFY_SYNTAX', evaluation: 'SYNTAX',
    title: 'Create a tuple',
    statement: 'Write a tuple containing the values 1, 2 and 3.',
    starter: 'point = {{STUDENT_CODE}}\n\nprint(point)',
    placeholder: '(...)',
    require: ['TUPLE_LITERAL'],
    solution: '(1, 2, 3)',
    explanation: 'A tuple is written with parentheses (or bare commas). Unlike a list it is immutable — once created, its contents cannot change.',
    hints: [
      'Tuples use round brackets, not square ones.',
      'Values are comma-separated.',
      'Write `(1, 2, 3)`.',
    ],
    tests: [],
    tags: ['tuples'],
  },
  {
    qid: 'PY-TUPLES-0002',
    language: 'python', topic: 'tuples', subtopic: 'unpacking', difficulty: 'Medium',
    title: 'Unpack a tuple',
    statement: 'Unpack the tuple `point` into three variables named `x`, `y` and `z`.',
    starter: 'point = (1, 2, 3)\n\n{{STUDENT_CODE}}\n\nprint(x, y, z)',
    placeholder: 'x, y, z = ...',
    require: ['UNPACKING'],
    forbid: ['SUBSCRIPT'],
    solution: 'x, y, z = point',
    explanation: 'Unpacking assigns each element of a sequence to a name in one statement. The number of names must match the number of elements exactly, or Python raises a ValueError.',
    hints: [
      'You do not need indexing for this.',
      'Put three names on the left of the `=`.',
      'Write `x, y, z = point`.',
    ],
    tags: ['tuples', 'unpacking'],
  },

  // -------------------------------------------------------- Python: sets
  {
    qid: 'PY-SETS-0001',
    language: 'python', topic: 'sets', subtopic: 'set-creation', difficulty: 'Medium',
    type: 'IDENTIFY_SYNTAX', evaluation: 'SYNTAX',
    title: 'Create a set',
    statement: 'Write a set containing the values 1, 2 and 3.',
    starter: 'unique = {{STUDENT_CODE}}\n\nprint(sorted(unique))',
    placeholder: '{...}',
    require: ['SET_LITERAL'],
    solution: '{1, 2, 3}',
    explanation: 'A set literal uses curly braces with plain values (no colons). Watch out: `{}` alone creates an empty *dictionary* — an empty set needs `set()`.',
    hints: [
      'Sets use the same brackets as dictionaries.',
      'But there are no key/value colons.',
      'Write `{1, 2, 3}`.',
    ],
    tests: [],
    tags: ['sets'],
  },
  {
    qid: 'PY-SETS-0002',
    language: 'python', topic: 'sets', subtopic: 'intersection', difficulty: 'Medium',
    title: 'Intersect two sets',
    statement: 'Store the values present in **both** `a` and `b` in a variable called `common`.',
    starter: 'a = {1, 2, 3, 4}\nb = {3, 4, 5}\n\n{{STUDENT_CODE}}\n\nprint(sorted(common))',
    placeholder: 'common = a & b',
    solution: 'common = a & b',
    altSolutions: ['common = a.intersection(b)'],
    explanation: 'The `&` operator (or `.intersection()`) returns the values common to both sets. `|` gives the union and `-` the difference.',
    hints: [
      'Sets support algebra operators.',
      'The one for "in both" is a single character.',
      'Write `common = a & b`.',
    ],
    tags: ['sets'],
  },

  // ----------------------------------------------------- Python: strings
  {
    qid: 'PY-STRING-0001',
    language: 'python', topic: 'strings', subtopic: 'slicing', difficulty: 'Medium',
    title: 'Slice a string',
    statement: 'Store the first three characters of `word` in `start`.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}\n\nprint(start)',
    placeholder: 'start = word[...]',
    require: ['SLICING'],
    solution: 'start = word[0:3]',
    altSolutions: ['start = word[:3]'],
    explanation: 'Strings slice exactly like lists: `word[:3]` returns a new string with the characters at positions 0, 1 and 2.',
    hints: [
      'Strings support the same slicing syntax as lists.',
      'The end index is excluded.',
      'Write `word[:3]`.',
    ],
    tests: [
      { setup: 'word = "python"', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'word = "hi"', expected: 'AUTO' },
    ],
    tags: ['strings', 'slicing'],
  },
  {
    qid: 'PY-STRING-0002',
    language: 'python', topic: 'strings', subtopic: 'string-methods', difficulty: 'Easy',
    type: 'COMPLETE_STATEMENT',
    title: 'Convert a string to upper case',
    statement: 'Complete the statement so `shout` holds the upper-case version of `word`.',
    starter: 'word = "hello"\n\nshout = word.{{STUDENT_CODE}}\n\nprint(shout)',
    placeholder: 'upper()',
    require: ['METHOD:upper'],
    solution: 'upper()',
    explanation: '`.upper()` returns a new upper-case string; strings are immutable, so the original `word` is unchanged.',
    hints: [
      'String methods are called with a dot and parentheses.',
      'The method name describes the case you want.',
      'Write `upper()`.',
    ],
    tags: ['strings', 'methods'],
  },

  // --------------------------------------------------------- Python: OOP
  {
    qid: 'PY-OOP-0001',
    language: 'python', topic: 'oop', subtopic: 'class-creation', difficulty: 'Medium',
    title: 'Define a class with a constructor',
    statement: 'Define a class `Dog` whose constructor stores the given `name` on the instance.',
    starter: '{{STUDENT_CODE}}\n\nd = Dog("Rex")\nprint(d.name)',
    placeholder: 'class Dog:\n    def __init__(self, name):\n        ...',
    require: ['CLASS_DEF', 'CONSTRUCTOR', 'SELF_PARAMETER'],
    solution: 'class Dog:\n    def __init__(self, name):\n        self.name = name',
    explanation: '`__init__` runs when the object is created. Its first parameter, conventionally `self`, is the new instance, and assigning to `self.name` stores the value on it.',
    hints: [
      'The constructor method has a special dunder name.',
      'Its first parameter is the instance itself.',
      'Assign to `self.name` inside it.',
    ],
    tags: ['oop', 'classes'],
  },
  {
    qid: 'PY-OOP-0002',
    language: 'python', topic: 'oop', subtopic: 'inheritance', difficulty: 'Hard',
    title: 'Inherit from a class',
    statement: 'Define `Cat` as a subclass of `Animal`, overriding `speak()` so it returns `"Meow"`.',
    starter: 'class Animal:\n    def speak(self):\n        return "..."\n\n{{STUDENT_CODE}}\n\nprint(Cat().speak())',
    placeholder: 'class Cat(Animal):\n    ...',
    require: ['CLASS_DEF', 'INHERITANCE', 'METHOD_DEF'],
    solution: 'class Cat(Animal):\n    def speak(self):\n        return "Meow"',
    explanation: 'The parent class goes in parentheses after the subclass name. Redefining a method with the same name overrides the inherited one — the essence of polymorphism.',
    hints: [
      'The base class is named in parentheses after the class name.',
      'Define a method with the same name to override it.',
      'Remember the `self` parameter.',
    ],
    tags: ['oop', 'inheritance'],
  },

  // -------------------------------------------------- Python: exceptions
  {
    qid: 'PY-EXCEPT-0001',
    language: 'python', topic: 'exceptions', subtopic: 'except', difficulty: 'Medium',
    title: 'Handle a division error',
    statement: 'Wrap the division so that a division by zero prints `Cannot divide by zero` instead of crashing.',
    starter: '{{SETUP_CODE}}\n\n{{STUDENT_CODE}}',
    placeholder: 'try:\n    ...\nexcept ...:\n    ...',
    require: ['TRY_EXCEPT'],
    solution: 'try:\n    print(a / b)\nexcept ZeroDivisionError:\n    print("Cannot divide by zero")',
    explanation: '`try` runs code that might fail; the matching `except` catches that specific exception type. Naming `ZeroDivisionError` rather than using a bare `except` keeps genuine bugs visible.',
    hints: [
      'The risky code goes in the `try` block.',
      'The handler names the exception class it catches.',
      'Division by zero raises `ZeroDivisionError`.',
    ],
    tests: [
      { setup: 'a = 10\nb = 2', expected: 'AUTO' },
      { setup: 'a = 10\nb = 0', expected: 'AUTO' },
      { visibility: 'hidden', setup: 'a = 0\nb = 5', expected: 'AUTO' },
    ],
    tags: ['exceptions'],
  },
  {
    qid: 'PY-EXCEPT-0002',
    language: 'python', topic: 'exceptions', subtopic: 'finally', difficulty: 'Hard',
    title: 'Add a finally block',
    statement: 'Add a `finally` block that always prints `Done`, whether or not the conversion succeeds.',
    starter: '{{SETUP_CODE}}\n\ntry:\n    number = int(value)\n    print(number)\nexcept ValueError:\n    print("Not a number")\n{{STUDENT_CODE}}',
    placeholder: 'finally:\n    print("Done")',
    require: ['FINALLY'],
    solution: 'finally:\n    print("Done")',
    explanation: '`finally` runs on every path out of the `try` — success, handled exception, even an unhandled one. It is where cleanup belongs.',
    hints: [
      'One keyword marks the block that always runs.',
      'It comes after the `except` block.',
      'Write `finally:` with the print indented under it.',
    ],
    tests: [
      { setup: 'value = "42"', expected: 'AUTO' },
      { setup: 'value = "abc"', expected: 'AUTO' },
    ],
    tags: ['exceptions'],
  },
];
