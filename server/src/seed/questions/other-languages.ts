import type { SeedQuestion } from './types.js';

/** §8 + §9 — Java, C, C++, JavaScript, HTML and CSS fragment questions. */
export const OTHER_LANGUAGES: SeedQuestion[] = [
  // --------------------------------------------------------------- Java
  {
    qid: 'JAV-LOOPS-0001',
    language: 'java', topic: 'loops', subtopic: 'for', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'Write a Java for loop header',
    statement: 'Write the `for` loop header that counts `i` from 0 up to (but not including) 10.',
    instructions: 'The loop body is already provided — write only the `for(...)` header line.',
    starter: 'public class Main {\n    public static void main(String[] args) {\n        {{STUDENT_CODE}}\n        {\n            System.out.println(i);\n        }\n    }\n}',
    placeholder: 'for (int i = 0; i < 10; i++)',
    require: ['FOR_LOOP'],
    solution: 'for (int i = 0; i < 10; i++)',
    explanation: 'A Java `for` header has three parts separated by semicolons: initialisation, the condition checked before each pass, and the update run after each pass. `i < 10` stops the loop at 9.',
    hints: [
      'The header has three sections separated by semicolons.',
      'They are: declare and initialise, test, then update.',
      'Write `for (int i = 0; i < 10; i++)`.',
    ],
    tags: ['java', 'loops'],
  },
  {
    qid: 'JAV-CONDIT-0001',
    language: 'java', topic: 'conditions', subtopic: 'if', difficulty: 'Easy',
    type: 'COMPLETE_CONDITION',
    title: 'Java: condition for an even number',
    statement: 'Write the condition that checks whether `num` is even.',
    starter: 'public class Main {\n    public static void main(String[] args) {\n        int num = 8;\n        if ({{STUDENT_CODE}}) {\n            System.out.println("Even");\n        } else {\n            System.out.println("Odd");\n        }\n    }\n}',
    placeholder: 'num % 2 == 0',
    require: ['OP_MOD', 'COMPARISON'],
    solution: 'num % 2 == 0',
    explanation: 'Java uses `%` for remainder and `==` for equality, exactly as Python does. Unlike Python, the condition must sit inside parentheses — but those are already provided here.',
    hints: [
      'Use the remainder operator.',
      'Compare the remainder against zero.',
      'Equality in Java is `==`, not `=`.',
    ],
    tags: ['java', 'conditions'],
  },
  {
    qid: 'JAV-ARRAYS-0001',
    language: 'java', topic: 'arrays', subtopic: 'declaration', difficulty: 'Medium',
    title: 'Declare a Java array',
    statement: 'Declare an `int` array called `numbers` containing 10, 20 and 30.',
    starter: 'public class Main {\n    public static void main(String[] args) {\n        {{STUDENT_CODE}}\n        for (int n : numbers) {\n            System.out.println(n);\n        }\n    }\n}',
    placeholder: 'int[] numbers = {10, 20, 30};',
    require: ['VARIABLE_DECLARATION'],
    solution: 'int[] numbers = {10, 20, 30};',
    altSolutions: ['int[] numbers = new int[]{10, 20, 30};'],
    explanation: 'Java arrays are declared with the element type followed by `[]`. The brace initialiser `{10, 20, 30}` may only be used at the point of declaration.',
    hints: [
      'The type comes first, then square brackets.',
      'Initial values go inside curly braces.',
      'Do not forget the semicolon.',
    ],
    tags: ['java', 'arrays'],
  },
  {
    qid: 'JAV-METHOD-0001',
    language: 'java', topic: 'methods', subtopic: 'definition', difficulty: 'Medium',
    title: 'Write a Java method body',
    statement: 'Complete the body of `square` so it returns `n` multiplied by itself.',
    starter: 'public class Main {\n    static int square(int n) {\n        {{STUDENT_CODE}}\n    }\n\n    public static void main(String[] args) {\n        System.out.println(square(4));\n        System.out.println(square(9));\n    }\n}',
    placeholder: 'return n * n;',
    require: ['RETURN'],
    solution: 'return n * n;',
    explanation: 'A method declared to return `int` must return an `int` on every path. `return n * n;` computes the value and hands it back to the caller.',
    hints: [
      'The method signature promises an `int` back.',
      'Use the `return` keyword.',
      'Statements end with a semicolon.',
    ],
    tags: ['java', 'methods'],
  },

  // ------------------------------------------------------------------- C
  {
    qid: 'C-CONDIT-0001',
    language: 'c', topic: 'conditions', subtopic: 'condition-expression', difficulty: 'Easy',
    type: 'COMPLETE_CONDITION',
    title: 'C: check whether a number is even',
    statement: 'Write the condition that checks whether `num` is even.',
    starter: '#include <stdio.h>\n\nint main() {\n    int num = 4;\n    if ({{STUDENT_CODE}}) {\n        printf("Even\\n");\n    } else {\n        printf("Odd\\n");\n    }\n    return 0;\n}',
    placeholder: 'num % 2 == 0',
    require: ['OP_MOD', 'COMPARISON'],
    solution: 'num % 2 == 0',
    explanation: 'C uses `%` for the remainder and `==` for comparison. A single `=` would assign instead of compare — a classic C bug that compiles without error.',
    hints: [
      'The remainder operator is `%`.',
      'Compare the remainder to zero.',
      'Use `==`, not `=`.',
    ],
    tags: ['c', 'conditions'],
  },
  {
    qid: 'C-LOOPS-0001',
    language: 'c', topic: 'loops', subtopic: 'for', difficulty: 'Easy',
    type: 'COMPLETE_LOOP',
    title: 'C: for loop header',
    statement: 'Write the `for` loop header that counts `i` from 1 to 5 inclusive.',
    starter: '#include <stdio.h>\n\nint main() {\n    int i;\n    {{STUDENT_CODE}}\n    {\n        printf("%d\\n", i);\n    }\n    return 0;\n}',
    placeholder: 'for (i = 1; i <= 5; i++)',
    require: ['FOR_LOOP'],
    solution: 'for (i = 1; i <= 5; i++)',
    explanation: 'The variable is already declared above, so the initialisation section only assigns. To include 5, the condition must be `<=` rather than `<`.',
    hints: [
      '`i` is already declared — do not declare it again.',
      'To include 5, use `<=`.',
      'The three sections are separated by semicolons.',
    ],
    tags: ['c', 'loops'],
  },
  {
    qid: 'C-BASICS-0001',
    language: 'c', topic: 'basics', subtopic: 'printf', difficulty: 'Easy',
    title: 'C: print a number',
    statement: 'Print the value of `count` followed by a newline, using printf.',
    starter: '#include <stdio.h>\n\nint main() {\n    int count = 7;\n    {{STUDENT_CODE}}\n    return 0;\n}',
    placeholder: 'printf("%d\\n", count);',
    require: ['PRINT'],
    solution: 'printf("%d\\n", count);',
    explanation: '`printf` uses format specifiers: `%d` for an int, followed by the value as the next argument. `\\n` adds the newline that `printf` does not add on its own.',
    hints: [
      'The format specifier for an integer is `%d`.',
      'The value to print comes after the format string.',
      'Add `\\n` for the newline.',
    ],
    tags: ['c', 'output'],
  },

  // ----------------------------------------------------------------- C++
  {
    qid: 'CPP-CONTAI-0001',
    language: 'cpp', topic: 'containers', subtopic: 'vector', difficulty: 'Medium',
    title: 'Create a C++ vector',
    statement: 'Create a vector of ints called `values` containing 10, 20 and 30.',
    starter: '#include <iostream>\n#include <vector>\n\nint main() {\n    {{STUDENT_CODE}}\n    for (int v : values) {\n        std::cout << v << "\\n";\n    }\n    return 0;\n}',
    placeholder: 'std::vector<int> values = {10, 20, 30};',
    require: ['VECTOR'],
    solution: 'std::vector<int> values = {10, 20, 30};',
    altSolutions: ['std::vector<int> values{10, 20, 30};'],
    explanation: '`std::vector<int>` declares a growable array of ints, and the brace initialiser fills it in one step. The element type goes inside the angle brackets.',
    hints: [
      'The type is `std::vector` with the element type in angle brackets.',
      'Brace initialisation fills it with values.',
      'End the statement with a semicolon.',
    ],
    tags: ['cpp', 'vector'],
  },
  {
    qid: 'CPP-LOOPS-0001',
    language: 'cpp', topic: 'loops', subtopic: 'range-for', difficulty: 'Medium',
    type: 'COMPLETE_LOOP',
    title: 'C++ range-based for loop',
    statement: 'Write a range-based `for` loop that prints every value in `values`, one per line.',
    starter: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> values = {1, 2, 3};\n    {{STUDENT_CODE}}\n    return 0;\n}',
    placeholder: 'for (int v : values) { ... }',
    require: ['RANGE_FOR'],
    solution: 'for (int v : values) {\n    std::cout << v << "\\n";\n}',
    explanation: 'The range-based `for` (`for (type name : container)`) walks a container without any index arithmetic — the C++ equivalent of Python\'s `for x in xs`.',
    hints: [
      'C++11 has a for loop that takes a container directly.',
      'The syntax uses a colon, not semicolons.',
      'Write `for (int v : values)` with a braced body.',
    ],
    tags: ['cpp', 'loops'],
  },
  {
    qid: 'CPP-BASICS-0001',
    language: 'cpp', topic: 'basics', subtopic: 'cout', difficulty: 'Easy',
    title: 'C++: print a value',
    statement: 'Print `name` followed by a newline using `std::cout`.',
    starter: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string name = "Ada";\n    {{STUDENT_CODE}}\n    return 0;\n}',
    placeholder: 'std::cout << name << "\\n";',
    require: ['PRINT'],
    solution: 'std::cout << name << "\\n";',
    explanation: '`std::cout` writes to standard output, and `<<` chains values onto the stream. Each `<<` sends one more thing, so the newline is a second `<<`.',
    hints: [
      'The output stream is `std::cout`.',
      'The insertion operator is `<<` and can be chained.',
      'Add `<< "\\n"` for the newline.',
    ],
    tags: ['cpp', 'output'],
  },

  // ---------------------------------------------------------- JavaScript
  {
    qid: 'JS-FUNCTI-0001',
    language: 'javascript', topic: 'functions', subtopic: 'arrow', difficulty: 'Easy',
    type: 'IDENTIFY_SYNTAX', evaluation: 'SYNTAX',
    title: 'Write an arrow function',
    statement: 'Write an arrow function that takes `n` and returns its square.',
    instructions: 'Write only the arrow function expression — it will be assigned to `square` for you.',
    starter: 'const square = {{STUDENT_CODE}};\n\nconsole.log(square(5));',
    placeholder: '(n) => n * n',
    require: ['ARROW_FUNCTION'],
    solution: '(n) => n * n',
    altSolutions: ['n => n * n', '(n) => n ** 2'],
    explanation: 'An arrow function with a single expression body returns that expression implicitly — no `return` and no braces needed. Parentheses around a single parameter are optional.',
    hints: [
      'Arrow functions use `=>` between parameters and body.',
      'A single-expression body returns automatically.',
      'Write `(n) => n * n`.',
    ],
    tests: [],
    tags: ['javascript', 'functions', 'arrow'],
  },
  {
    qid: 'JS-ARRAYS-0001',
    language: 'javascript', topic: 'arrays', subtopic: 'map', difficulty: 'Medium',
    title: 'Transform an array with map',
    statement: 'Create `doubled` — every number in `numbers` multiplied by 2 — using `.map()`.',
    starter: 'const numbers = [1, 2, 3];\n\n{{STUDENT_CODE}}\n\nconsole.log(doubled.join(","));',
    placeholder: 'const doubled = numbers.map(...)',
    require: ['METHOD:map'],
    forbid: ['FOR_LOOP'],
    config: { constructMessage: 'This question is practising `.map()` — build the new array with it rather than with a loop.' },
    solution: 'const doubled = numbers.map((n) => n * 2);',
    explanation: '`.map()` returns a new array of the same length, with the callback applied to every element. It never modifies the original array.',
    hints: [
      'There is an array method that transforms every element.',
      'It takes a callback and returns a new array.',
      'Write `numbers.map((n) => n * 2)`.',
    ],
    tags: ['javascript', 'arrays', 'map'],
  },
  {
    qid: 'JS-ARRAYS-0002',
    language: 'javascript', topic: 'arrays', subtopic: 'filter', difficulty: 'Medium',
    title: 'Filter an array',
    statement: 'Create `evens` containing only the even numbers from `numbers`, using `.filter()`.',
    starter: 'const numbers = [1, 2, 3, 4, 5, 6];\n\n{{STUDENT_CODE}}\n\nconsole.log(evens.join(","));',
    placeholder: 'const evens = numbers.filter(...)',
    require: ['METHOD:filter'],
    forbid: ['FOR_LOOP'],
    solution: 'const evens = numbers.filter((n) => n % 2 === 0);',
    explanation: '`.filter()` keeps only the elements for which the callback returns a truthy value. JavaScript\'s strict equality is `===`; `==` would apply type coercion.',
    hints: [
      'The method keeps elements whose callback returns true.',
      'Even means the remainder after dividing by 2 is 0.',
      'Prefer `===` over `==` in JavaScript.',
    ],
    tags: ['javascript', 'arrays', 'filter'],
  },
  {
    qid: 'JS-BASICS-0001',
    language: 'javascript', topic: 'basics', subtopic: 'template-literals', difficulty: 'Easy',
    title: 'Use a template literal',
    statement: 'Log exactly `Ada is 36` using a template literal that inserts both variables.',
    starter: 'const name = "Ada";\nconst age = 36;\n\n{{STUDENT_CODE}}',
    placeholder: 'console.log(`...`)',
    require: ['TEMPLATE_LITERAL', 'PRINT'],
    solution: 'console.log(`${name} is ${age}`);',
    explanation: 'Template literals are written with backticks, and `${expression}` interpolates a value. They are the JavaScript equivalent of Python f-strings.',
    hints: [
      'Template literals use backticks, not quotes.',
      'Values are inserted with `${...}`.',
      'Write `console.log(`${name} is ${age}`)`.',
    ],
    tags: ['javascript', 'strings'],
  },

  // ---------------------------------------------------------------- HTML
  {
    qid: 'HTM-ELEMEN-0001',
    language: 'html', topic: 'elements', subtopic: 'links', difficulty: 'Easy',
    evaluation: 'STATIC',
    title: 'Create a hyperlink',
    statement: 'Create a hyperlink to `https://google.com` whose visible text is `Google`.',
    starter: '<body>\n    {{STUDENT_CODE}}\n</body>',
    placeholder: '<a href="...">...</a>',
    require: ['TAG:a'],
    config: {
      html: { tag: 'a', attributes: { href: 'https://google.com' }, textEquals: 'Google' },
    },
    solution: '<a href="https://google.com">Google</a>',
    explanation: 'The anchor element `<a>` creates a link. The destination goes in the `href` attribute, and the text between the opening and closing tags is what the reader clicks.',
    hints: [
      'The tag for a link is a single letter.',
      'The destination goes in the `href` attribute.',
      'The visible text sits between the opening and closing tags.',
    ],
    tests: [],
    tags: ['html', 'links'],
  },
  {
    qid: 'HTM-ELEMEN-0002',
    language: 'html', topic: 'elements', subtopic: 'images', difficulty: 'Easy',
    evaluation: 'STATIC',
    title: 'Add an image with alt text',
    statement: 'Add an image loading `logo.png` with the alternative text `Company logo`.',
    starter: '<body>\n    {{STUDENT_CODE}}\n</body>',
    placeholder: '<img src="..." alt="...">',
    require: ['TAG:img'],
    config: {
      html: { tag: 'img', attributes: { src: 'logo.png', alt: 'Company logo' } },
    },
    solution: '<img src="logo.png" alt="Company logo">',
    explanation: '`<img>` is a void element — it has no closing tag. `src` is the file to load and `alt` is the text shown when the image cannot be displayed, which screen readers also announce.',
    hints: [
      'The image tag needs no closing tag.',
      'Two attributes are required here.',
      '`src` is the file, `alt` is the description.',
    ],
    tests: [],
    tags: ['html', 'images', 'accessibility'],
  },
  {
    qid: 'HTM-ELEMEN-0003',
    language: 'html', topic: 'elements', subtopic: 'lists', difficulty: 'Medium',
    evaluation: 'STATIC',
    title: 'Build an unordered list',
    statement: 'Create an unordered list with exactly three items: `Red`, `Green` and `Blue`.',
    starter: '<body>\n    {{STUDENT_CODE}}\n</body>',
    placeholder: '<ul>\n  <li>...</li>\n</ul>',
    require: ['TAG:ul', 'TAG:li'],
    config: {
      html: { selector: 'li', minCount: 3 },
    },
    solution: '<ul>\n    <li>Red</li>\n    <li>Green</li>\n    <li>Blue</li>\n</ul>',
    explanation: '`<ul>` is the bulleted list container and each entry is an `<li>` element nested inside it. An ordered (numbered) list would use `<ol>` with the same `<li>` children.',
    hints: [
      'The container tag for a bulleted list is two letters.',
      'Each item uses its own tag inside the container.',
      'You need three item elements.',
    ],
    tests: [],
    tags: ['html', 'lists'],
  },
  {
    qid: 'HTM-FORMS-0001',
    language: 'html', topic: 'forms', subtopic: 'input', difficulty: 'Medium',
    evaluation: 'STATIC',
    title: 'Create a text input',
    statement: 'Create a text input whose `name` is `username` and whose placeholder text is `Enter username`.',
    starter: '<form>\n    {{STUDENT_CODE}}\n</form>',
    placeholder: '<input type="text" ...>',
    require: ['TAG:input'],
    config: {
      html: { tag: 'input', attributes: { type: 'text', name: 'username', placeholder: 'Enter username' } },
    },
    solution: '<input type="text" name="username" placeholder="Enter username">',
    explanation: '`<input>` covers most form fields; `type` selects which kind. The `name` attribute is the key the value is submitted under, and `placeholder` is the hint shown in an empty field.',
    hints: [
      'A text field is an `<input>` with a `type` attribute.',
      'Three attributes are needed: type, name and placeholder.',
      'Attribute order does not matter.',
    ],
    tests: [],
    tags: ['html', 'forms'],
  },

  // ----------------------------------------------------------------- CSS
  {
    qid: 'CSS-TEXT-0001',
    language: 'css', topic: 'text', subtopic: 'font', difficulty: 'Easy',
    evaluation: 'STATIC',
    title: 'Make text bold',
    statement: 'Write the declaration that makes the paragraph text bold.',
    instructions: 'The selector and braces are provided — write only the declaration.',
    starter: 'p {\n    {{STUDENT_CODE}}\n}',
    placeholder: 'font-weight: bold;',
    require: ['PROPERTY:font-weight'],
    config: {
      css: { declarations: { 'font-weight': 'bold' } },
    },
    solution: 'font-weight: bold;',
    altSolutions: ['font-weight: 700;'],
    explanation: 'A CSS declaration is `property: value;`. `font-weight: bold` is equivalent to `font-weight: 700` — the keyword and the numeric scale describe the same weight.',
    hints: [
      'The property controls the weight of the font.',
      'It is written with a hyphen.',
      'Write `font-weight: bold;`.',
    ],
    tests: [],
    tags: ['css', 'text'],
  },
  {
    qid: 'CSS-TEXT-0002',
    language: 'css', topic: 'text', subtopic: 'alignment', difficulty: 'Easy',
    evaluation: 'STATIC',
    title: 'Centre text',
    statement: 'Write the declaration that centres the heading text horizontally.',
    starter: 'h1 {\n    {{STUDENT_CODE}}\n}',
    placeholder: 'text-align: center;',
    require: ['PROPERTY:text-align'],
    config: {
      css: { declarations: { 'text-align': 'center' } },
    },
    solution: 'text-align: center;',
    explanation: '`text-align` positions inline content inside its block. It aligns the text within the element\'s box — it does not move the box itself.',
    hints: [
      'The property name mentions both text and alignment.',
      'The value uses the American spelling.',
      'Write `text-align: center;`.',
    ],
    tests: [],
    tags: ['css', 'text'],
  },
  {
    qid: 'CSS-BOXMOD-0001',
    language: 'css', topic: 'box-model', subtopic: 'padding', difficulty: 'Medium',
    evaluation: 'STATIC',
    title: 'Add padding',
    statement: 'Give the card 16 pixels of padding on every side, in a single declaration.',
    starter: '.card {\n    {{STUDENT_CODE}}\n}',
    placeholder: 'padding: 16px;',
    require: ['PROPERTY:padding'],
    config: {
      css: { declarations: { padding: '16px' } },
    },
    solution: 'padding: 16px;',
    explanation: 'The `padding` shorthand with one value applies that spacing to all four sides. Padding is the space *inside* the border, between it and the content.',
    hints: [
      'One shorthand property covers all four sides.',
      'A single value applies everywhere.',
      'Remember the `px` unit.',
    ],
    tests: [],
    tags: ['css', 'box-model'],
  },
  {
    qid: 'CSS-SELECT-0001',
    language: 'css', topic: 'selectors', subtopic: 'class', difficulty: 'Medium',
    evaluation: 'STATIC',
    title: 'Write a class selector rule',
    statement: 'Write a complete rule that gives every element with the class `highlight` a yellow background.',
    instructions: 'Write the whole rule this time — selector, braces and declaration.',
    starter: '{{STUDENT_CODE}}',
    placeholder: '.highlight {\n  background-color: yellow;\n}',
    require: ['CLASS_SELECTOR', 'PROPERTY:background-color'],
    config: {
      css: { selector: '.highlight', declarations: { 'background-color': 'yellow' } },
    },
    solution: '.highlight {\n    background-color: yellow;\n}',
    explanation: 'A class selector is the class name prefixed with a dot. The declarations that apply to it go inside the braces that follow.',
    hints: [
      'Class selectors start with a punctuation mark.',
      'That mark is a full stop.',
      'The declarations go between `{` and `}`.',
    ],
    tests: [],
    tags: ['css', 'selectors'],
  },
  {
    qid: 'CSS-LAYOUT-0001',
    language: 'css', topic: 'layout', subtopic: 'flexbox', difficulty: 'Medium',
    evaluation: 'STATIC',
    title: 'Centre with flexbox',
    statement: 'Turn the container into a flex container and centre its children both horizontally and vertically. Three declarations are needed.',
    starter: '.container {\n    {{STUDENT_CODE}}\n}',
    placeholder: 'display: flex;\njustify-content: center;\nalign-items: center;',
    require: ['PROPERTY:display', 'PROPERTY:justify-content', 'PROPERTY:align-items'],
    config: {
      css: {
        declarations: { display: 'flex', 'justify-content': 'center', 'align-items': 'center' },
      },
    },
    solution: 'display: flex;\njustify-content: center;\nalign-items: center;',
    explanation: '`display: flex` starts a flex context. `justify-content` positions items along the main axis (horizontal by default) and `align-items` along the cross axis, so together they centre in both directions.',
    hints: [
      'First switch the container to flex layout.',
      'One property handles the main axis, another the cross axis.',
      'Both centring properties take the value `center`.',
    ],
    tests: [],
    tags: ['css', 'flexbox', 'layout'],
  },
];
