import type { SeedQuestion } from './types.js';

/** Python — Functions (10). */
export const PYTHON_FUNCTIONS: SeedQuestion[] = [
  {
    qid: 'PY-FUNCTI-0001',
    language: 'python', topic: 'functions', subtopic: 'function-definition', difficulty: 'Easy',
    title: 'Define a function',
    statement: 'Define a function called `greet` that takes no parameters and prints `Hello`.',
    objective: 'Write a function definition header and body.',
    starter: '{{STUDENT_CODE}}\n\ngreet()',
    placeholder: 'def greet():\n    ...',
    require: ['FUNCTION_DEF', 'DEF:greet'],
    solution: 'def greet():\n    print("Hello")',
    explanation: 'A function definition is `def name():` followed by an indented body. Defining it does nothing on its own — the call `greet()` at the bottom is what runs the body.',
    hints: [
      'Function definitions start with a keyword three letters long.',
      'The header ends with parentheses and a colon.',
      'Write `def greet():` with an indented `print("Hello")`.',
    ],
    tags: ['functions', 'def'],
  },
  {
    qid: 'PY-FUNCTI-0002',
    language: 'python', topic: 'functions', subtopic: 'parameters', difficulty: 'Easy',
    title: 'Add a parameter',
    statement: 'Define `greet(name)` so that calling `greet("Ada")` prints `Hello, Ada`.',
    starter: '{{STUDENT_CODE}}\n\ngreet("Ada")\ngreet("Grace")',
    placeholder: 'def greet(name):\n    ...',
    require: ['FUNCTION_DEF', 'PARAMETER'],
    solution: 'def greet(name):\n    print(f"Hello, {name}")',
    explanation: 'Parameters are named inside the parentheses of the `def` line and behave like local variables inside the body, taking whatever value the caller passed.',
    hints: [
      'The parameter name goes inside the parentheses.',
      'Inside the body, use it like a normal variable.',
      'An f-string makes building the message easy.',
    ],
    tags: ['functions', 'parameters'],
  },
  {
    qid: 'PY-FUNCTI-0003',
    language: 'python', topic: 'functions', subtopic: 'return', difficulty: 'Easy',
    title: 'Return a value',
    statement: 'Define `square(n)` that **returns** n multiplied by itself. Do not print inside the function.',
    starter: '{{STUDENT_CODE}}\n\nprint(square(4))\nprint(square(9))',
    placeholder: 'def square(n):\n    return ...',
    require: ['FUNCTION_DEF', 'RETURN'],
    forbid: ['PRINT'],
    config: { constructMessage: 'This function must return its value rather than print it — the caller does the printing.' },
    solution: 'def square(n):\n    return n * n',
    altSolutions: ['def square(n):\n    return n ** 2'],
    explanation: '`return` hands a value back to the caller so it can be stored, printed or used in further maths. A function that prints instead of returning cannot be composed into other expressions.',
    hints: [
      'Returning and printing are different things.',
      'The keyword sends the value back to whoever called the function.',
      'Write `return n * n`.',
    ],
    tags: ['functions', 'return'],
  },
  {
    qid: 'PY-FUNCTI-0004',
    language: 'python', topic: 'functions', subtopic: 'parameters', difficulty: 'Medium',
    title: 'Two parameters',
    statement: 'Define `add(a, b)` that returns the sum of its two arguments.',
    starter: '{{STUDENT_CODE}}\n\nprint(add(2, 3))\nprint(add(-1, 1))',
    placeholder: 'def add(a, b):\n    return ...',
    require: ['FUNCTION_DEF', 'RETURN'],
    solution: 'def add(a, b):\n    return a + b',
    explanation: 'Multiple parameters are separated by commas in the `def` line, and arguments are matched to them by position at the call site.',
    hints: [
      'Separate the two parameter names with a comma.',
      'Return their sum rather than printing it.',
      'Write `def add(a, b):` then `return a + b`.',
    ],
    tags: ['functions', 'parameters'],
  },
  {
    qid: 'PY-FUNCTI-0005',
    language: 'python', topic: 'functions', subtopic: 'default-parameters', difficulty: 'Medium',
    title: 'Give a parameter a default value',
    statement: 'Define `greet(name, greeting="Hello")` so that the greeting can be left out by the caller.',
    starter: '{{STUDENT_CODE}}\n\nprint(greet("Ada"))\nprint(greet("Ada", "Hi"))',
    placeholder: 'def greet(name, greeting="Hello"):\n    return ...',
    require: ['FUNCTION_DEF', 'DEFAULT_PARAMETER', 'RETURN'],
    solution: 'def greet(name, greeting="Hello"):\n    return f"{greeting}, {name}"',
    explanation: 'A default is written as `parameter=value` in the `def` line and is used whenever the caller omits that argument. Parameters with defaults must come after those without.',
    hints: [
      'Defaults are written with `=` in the definition, not the call.',
      'The parameter with a default must come last.',
      'Write `def greet(name, greeting="Hello"):`.',
    ],
    tags: ['functions', 'defaults'],
  },
  {
    qid: 'PY-FUNCTI-0006',
    language: 'python', topic: 'functions', subtopic: 'return', difficulty: 'Medium',
    title: 'Return two values',
    statement: 'Define `min_max(numbers)` that returns the smallest and the largest number, in that order.',
    starter: '{{STUDENT_CODE}}\n\nlow, high = min_max([4, 9, 1, 7])\nprint(low, high)',
    placeholder: 'def min_max(numbers):\n    return ...',
    require: ['FUNCTION_DEF', 'RETURN'],
    solution: 'def min_max(numbers):\n    return min(numbers), max(numbers)',
    explanation: 'Returning several comma-separated values builds a tuple, which the caller can unpack into separate names — `low, high = min_max(...)`.',
    hints: [
      'A `return` can list more than one value.',
      'Separate them with a comma; Python makes a tuple.',
      'Built-ins `min()` and `max()` do the work.',
    ],
    tags: ['functions', 'tuples', 'return'],
  },
  {
    qid: 'PY-FUNCTI-0007',
    language: 'python', topic: 'functions', subtopic: 'lambda', difficulty: 'Medium',
    type: 'IDENTIFY_SYNTAX', evaluation: 'SYNTAX',
    title: 'Write a lambda',
    statement: 'Write a lambda expression that takes one argument `n` and returns `n * 3`.',
    instructions: 'Write only the lambda expression itself — it will be assigned to `triple` for you.',
    starter: 'triple = {{STUDENT_CODE}}\n\nprint(triple(5))',
    placeholder: 'lambda n: ...',
    require: ['LAMBDA'],
    solution: 'lambda n: n * 3',
    explanation: 'A lambda is a one-expression anonymous function: `lambda parameters: expression`. Its value is returned automatically — writing `return` inside a lambda is a syntax error.',
    hints: [
      'Anonymous functions use a dedicated keyword.',
      'The parameters come before a colon, the expression after.',
      'The shape is `lambda n: expression`.',
    ],
    tests: [],
    tags: ['functions', 'lambda'],
  },
  {
    qid: 'PY-FUNCTI-0008',
    language: 'python', topic: 'functions', subtopic: 'args', difficulty: 'Hard',
    title: 'Accept any number of arguments',
    statement: 'Define `total(*numbers)` that returns the sum of however many numbers it is given.',
    starter: '{{STUDENT_CODE}}\n\nprint(total(1, 2, 3))\nprint(total(10))\nprint(total())',
    placeholder: 'def total(*numbers):\n    return ...',
    require: ['FUNCTION_DEF', 'ARGS', 'RETURN'],
    solution: 'def total(*numbers):\n    return sum(numbers)',
    explanation: 'A parameter prefixed with `*` collects every extra positional argument into a tuple, so the function works with zero, one or many arguments.',
    hints: [
      'One symbol before a parameter name makes it variadic.',
      'Inside the function the parameter is a tuple.',
      'Write `def total(*numbers):` then `return sum(numbers)`.',
    ],
    tags: ['functions', 'args'],
  },
  {
    qid: 'PY-FUNCTI-0009',
    language: 'python', topic: 'functions', subtopic: 'kwargs', difficulty: 'Hard',
    title: 'Accept keyword arguments',
    statement: 'Define `describe(**details)` that prints each keyword argument as `key=value`, one per line.',
    starter: '{{STUDENT_CODE}}\n\ndescribe(name="Ada", role="engineer")',
    placeholder: 'def describe(**details):\n    ...',
    require: ['FUNCTION_DEF', 'KWARGS'],
    solution: 'def describe(**details):\n    for key, value in details.items():\n        print(f"{key}={value}")',
    explanation: '`**` collects arbitrary keyword arguments into a dictionary, so the function can be called with any set of named values and loop over them with `.items()`.',
    hints: [
      'Two symbols before a parameter name collect keyword arguments.',
      'Inside the function the parameter is a dictionary.',
      'Loop over it with `.items()`.',
    ],
    tags: ['functions', 'kwargs'],
  },
  {
    qid: 'PY-FUNCTI-0010',
    language: 'python', topic: 'functions', subtopic: 'return', difficulty: 'Hard',
    title: 'Return early from a function',
    statement: 'Define `first_even(numbers)` that returns the first even number in the list, or `None` when there is none.',
    starter: '{{STUDENT_CODE}}\n\nprint(first_even([1, 3, 6, 8]))\nprint(first_even([1, 3, 5]))',
    placeholder: 'def first_even(numbers):\n    ...',
    require: ['FUNCTION_DEF', 'FOR_LOOP', 'RETURN'],
    solution: 'def first_even(numbers):\n    for n in numbers:\n        if n % 2 == 0:\n            return n\n    return None',
    explanation: '`return` exits the function immediately, so returning from inside a loop stops the search at the first match. The final `return None` covers the case where the loop finishes without finding anything — and is what Python returns implicitly anyway.',
    hints: [
      'A `return` inside a loop ends the whole function, not just the loop.',
      'Return as soon as you find an even number.',
      'After the loop, return `None` for the "not found" case.',
    ],
    tags: ['functions', 'early-return', 'loops'],
  },
];
